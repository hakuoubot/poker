# 引き継ぎメモ(HANDOFF)

このファイルは設計判断・近似・ハマったところの記録。README.md より詳しい。
作業を引き継ぐときはまずここを読む(`ma-jyan/HANDOFF.md` と同じ役割)。

## プロジェクトの目的(2026-09-08 開始)

麻雀の打牌アドバイザー(`D:\karinamamono\ma-jyan`)のポーカー版がほしい、
という話から始まった。麻雀版が「切る牌を優先度順に出す」のと同じ感覚で、
ポーカーでは「フォールド/コール/レイズを優先度順に出す」ツールにする。

決めたこと(最初に確認した):

- **テキサスホールデム**、**6人リング(6-max)のキャッシュゲーム**前提
- GUI 一式まで作る(麻雀版と同じ、カードをクリックで入力する形)

麻雀版から引き継いだ方針:

- **すべてローカル完結。外部通信は一切しない。** Claude API も使わない。
  勝率計算もハンド評価も自前。追加ライブラリも無し(標準ライブラリのみ)。
- 第三者のポーカーサービスへの自動操作・画面読み取りは作らない
  (規約違反になる)。自作アプリ or 自分の勉強用という前提。

## 現在の実装状況(2026-09-08 時点)

全部できていて、検証も通っている。

| ファイル | 中身 | 状態 |
| --- | --- | --- |
| `cards.py` | カードの表現とパース | 完成 |
| `evaluator.py` | 5〜7枚の役評価 | 完成・検証済み |
| `equity.py` | 勝率(全数え上げ / モンテカルロ) | 完成・検証済み |
| `draws.py` | 今の役とアウツ | 完成 |
| `preflop.py` | レンジ表とレンジ表記のパーサ | 完成 |
| `strategy.py` | アクションを期待値順に並べる | 完成(近似あり、下記) |
| `app.py` | tkinter GUI | 完成 |
| `advisor.py` | 対話式CLI | 完成 |
| `test_evaluator.py` / `test_equity.py` | 検証 | 通っている |

## 設計判断

### カードを整数にした

麻雀版は牌を `"1m"` のような文字列で持っているが、ポーカーは
モンテカルロで数万回まわすので、最初から
`card = ランク番号 * 4 + スート番号` の 0〜51 の整数にした。
ビット演算で役判定ができるので速度が段違いになる。

### 評価器は C(7,5) の総当たりをやめた

7枚から5枚を選ぶ21通りを全部評価する素直な実装だと、
モンテカルロ2万回 x (自分+相手) で100万回以上の5枚評価になって遅い。

代わりに、ランクの出現数の配列とスート別の13bitマスクから直接役を決めている。
ストレートは 8192 通りのマスク→最高位ランクの表を起動時に作って引くだけ。
結果 **1秒あたり十数万〜30万回**(Python純正、実測値。マシンの負荷で振れる)。
2万回のモンテカルロが 0.3〜1秒で終わる。

検証は `test_evaluator.py` で、まったく別実装の素朴な総当たり評価器と
2万局面つき合わせている。見ているのは役の絶対値ではなく
**2つの手の優劣が一致するか**(引き分けも含めて)。ここが合っていれば実用上正しい。

- 途中で「7枚ならフラッシュとフルハウスが同居しうるから max を取らないと」と
  考えて max を入れたが、よく考えると **7枚では同居しない**。
  同じスートは各ランク1枚しかないので、3枚+2枚を作ると必ずスートが割れる。
  max のままでも間違いではないので残してある(コメントに理由を書いた)。

### 勝率は「数えられるなら数える」

- リバー(相手1人): C(45,2)=990通り → 全数え上げ、真値
- ターン(相手1人): 46 x 990 = 45,540通り → 全数え上げ、真値
- フロップ以降で組み合わせが 12万通りを超える場合 → モンテカルロ

`exact_limit` で線を引いている。全数え上げのときは `EquityResult.exact=True` が
立って、画面にも「正確な値」と出る。

検証(`test_equity.py`)は2段構え:

1. 同じ局面でモンテカルロと全数え上げを比べる(MC の実装ミスはここで出る)
2. プリフロップの勝率を公表値と比べる(AA 85.2%、72o 34.6%、AKs 67.0% など)

実測は AA 85.4% / KK 82.6% / 72o 34.4% / AKs 67.1% / AA vs 5人 49.3% で、
どれも公表値と 0.2 ポイント以内。

### 相手のレンジを入れた

「相手は完全ランダム」で計算すると自分に甘い数字が出る。
ベットに対して降りるかどうかの判断には使えない。

そこで `equity()` に `opp_range`(相手が持ちうる2枚の組み合わせのリスト)を
渡せるようにして、`preflop.py` のポジション別オープンレンジを既定で使っている。
GUI のチェックを外すとランダム相手に戻せる。

### レンジ表の広さ

一般的な 6-max のチャートに合わせて、実際の割合を測りながら調整した:

| 位置 | 割合 |
| --- | --- |
| UTG | 14.3% |
| HJ | 18.9% |
| CO | 27.0% |
| BTN | 45.7% |
| SB | 38.8% |

`preflop.range_percent()` で測れる。レンジをいじったらこの数字を見て、
一般的な広さから外れていないか確認する。

レイズに対する対応(3ベット/コール)は、相手の位置を EP(UTG,HJ) /
LP(CO,BTN) / SB の3グループにまとめ、自分にポジションがあるかで分けている。
ソルバーは席ごとに細かく変えるが、実戦で使う粒度としてこのくらいに寄せた。

### アクションの期待値

すでにポットに入れた分はサンクコストなので、**降りる = 0** を基準にする。

```
EV(コール) = 勝率 x ポット - (1 - 勝率) x 払う額
EV(レイズ) = f x ポット + (1 - f) x { 勝率' x (ポット + R - 払う額) - (1 - 勝率') x R }
EV(チェック) = 勝率 x ポット
```

- `f`(相手が降りる確率)は **MDF**。
  「相手が守るべき最低頻度」から出しているので、
  相手がセオリー通りに降りる前提の値。
- `勝率'` は、相手のレンジを **今のボードでの手の強さ順** に並べて、
  上から `(1-f)` の割合だけが残ると考えて計算し直したもの。
  ベットすると弱い手が降りて相手のレンジが強くなる、という効果が入る。
  プリフロップはボードが無いので、並べ替えに **Chen フォーミュラ** を使っている。

この「上位 (1-f) が続けてくる」という置き方が、この設計のキモ。
単に「勝率はそのまま、相手が f で降りる」とすると、
大きく張るほど得という明らかに間違った答えが出る。

### 分かっている近似(重要)

1. **以降のストリートのベットを見ていない。** インプライドオッズも
   リバースインプライドオッズも入っていない。ドローハンドの EV は低めに出る。
2. **大きいベットの評価が甘い。** MDF は「相手がセオリー通り」の値なので、
   オールインのような大きい額でも理屈上たくさん降りることになる。
   実戦では大きく行くほど降りずに強い手だけが来るので、
   オールインの EV は高めに出やすい。**画面にも毎回この注意を出している。**
3. **相手のレンジがストリートをまたいで絞られない。** フロップで降りなかった
   相手は本当はもっと強いはずだが、そこは追っていない。
4. **複数人相手は近似。** 全員が同じレンジで、独立に同じ確率で降りるとみなす。
   実際は誰か1人が強ければ他も降りにくい、といった相関がある。
5. **プリフロップの EV は参考値。** プリフロップはレンジ表に従うのが実戦的なので、
   GUI では期待値の上にレンジ表の推奨を別枠で出し、
   「レンジ表の方を優先して良い」と明示している。

### アウツの数え方で1回直した

最初は「次の1枚で役のカテゴリが上がるカード」を全部アウツにしていたが、
それだと AK で 9-8-2 のボードに対して **15枚** という数字が出た。
2 や 7 を引いて「ボードのペア」ができるのもカウントしていたため。
それは全員が同じ役になるだけでアウツではない。

`draws.board_category()` を足して、
**場のカードだけで作れる役より強くなる場合のみ**数えるようにした。
同じ局面が 6枚(A と K の3枚ずつ)になり、実戦の感覚と一致した。

この直しで、自分がスリーカードのときにボードがペアになってフルハウスになる、
のような「ボードのペアが自分だけの利益になる」ケースはちゃんと残る。

## ハマったところ・環境メモ

- **コンソールに ♠♥♦♣ が出せない。** Windows のコンソールが cp932 だと
  `UnicodeEncodeError` になる。GUI(tkinter)は問題ない。
  CLI(`advisor.py`)は英字表記(`As`)を使い、`sys.stdout.reconfigure(encoding="utf-8")`
  も入れてある。テストを流すときは `PYTHONIOENCODING=utf-8` を付けると読める。
- **GUI の初期サイズ。** 1280x800 の画面で右側が切れた。
  `winfo_reqwidth()` で必要幅を測りながら、カードパレットのフォントと
  結果表の列幅を詰めて、必要幅 1007px に収めた(ウィンドウは 1120x760)。
- 計算は別スレッドで走らせて、`queue` 経由で GUI に返している(麻雀版と同じ形)。
  入力が変わったら世代番号を上げて、走っている計算は `should_stop` で捨てる。

## 次にやると良さそうなこと(優先度順の目安)

1. **相手のレンジを画面で編集できるようにする。** 169マスのグリッドを出して
   クリックで足し引きする。今はポジションから自動で決めているだけなので、
   相手の癖に合わせられない。効きが一番大きいのはここ。
2. **ストリートをまたいだレンジの絞り込み。** 「フロップのベットに付いてきた」
   という情報でレンジを狭める。近似3の解消。
3. **インプライドオッズ。** ドローが完成したときに追加で取れる額を
   ざっくり見積もって EV に足す。近似1の解消。
4. ハンド履歴の保存と振り返り(あとで「あの局面どうだった」を見返せるように)
5. 自作ポーカーアプリの画面からカードを読み取る(麻雀版と同じ路線。
   麻雀版ではテンプレートマッチングの精度に苦戦しているので、
   カードは絵柄がはっきりしている分やりやすいはず)

---

## 追記1: GTOソルバー(TexasSolver)の導入(2026-09-09)

「このツールはポーカーの実力として何級か」という話から、
**足りないのはレンジ推定とバランスで、それはソルバーを積めば埋まる**という結論になり導入した。
麻雀版で akochan を積んだのと同じ位置づけ。

### なぜ TexasSolver か

- https://github.com/bupticybee/TexasSolver 、**AGPL-3.0**。
  akochan の独自規約と違い普通の OSS ライセンスだが、
  **同梱・再配布はしない**方針は同じ(`solver_engine/README.md` の手順で各自置く)。
- **Windows のビルド済みバイナリが公式リリースにある**(v0.2.0, 41.4MB)。
  akochan のときのような Docker ビルドが要らない。ここが決め手。
- 実行時はオフラインで動く。ローカル完結の前提を崩さない。

### 使い方: console_solver.exe

GUI版とは別に `console_solver.exe -i コマンドファイル` で動く。
標準入力ではなく**ファイルにコマンドを並べて渡す**方式。

```
set_pot 6
set_effective_stack 97
set_board Qs,8d,7h
set_range_ip AA,KK,AKs,...
set_range_oop ...
set_bet_sizes oop,flop,bet,50
build_tree
set_thread_num 14
set_accuracy 1.0
set_max_iteration 60
start_solve
set_dump_rounds 1
dump_result result.json
```

出力 JSON は木構造で、各ノードに
`{"actions": [...], "strategy": {"AsKh": [頻度, 頻度, ...]}}` が入っている。
**木の根は必ず OOP(先に行動する側)の判断**。自分が IP で相手がチェックしたなら
`childrens["CHECK"]`、相手がベットしたなら `childrens["BET x"]` を見る
(`solver_bridge.node_path_for()`)。

手札のキーは `4d3c` のように **ランク降順・同ランクはスート降順(s>h>d>c)**。
こちらのカード整数は `ランク*4 + スート(c,d,h,s)` なので、
**整数の降順に並べるだけで一致する**。

### 速度(14スレッド / 16GB、実測)

| ストリート | 時間 | 残り搾取率 |
| --- | --- | --- |
| フロップ | 82秒 | 0.67% |
| ターン | 7秒 | 1.3% |
| リバー | 数秒 | — |

**反復を削って速くしてはいけない。** 25反復まで削ったら同じ局面の答えが
「ベット61%/チェック39%」になった(十分回すと「ベット96〜98%」)。
**速くするなら木の枝を削る。** ベットサイズを1種類にし、レイズを
今のストリートだけに限定したら 269秒 → 82秒 になり、答えは収束版と一致した。

### GUI への組み込み

麻雀版の akochan と同じ形。

- **手動ボタン**(「ソルバーに聞く」)。自動実行にすると入力のたびに80秒走ることになる。
- 別スレッド + `queue` で受ける。Tkinter のウィジェットは本スレッドからのみ触る。
- 計算中はボタンが「中止」に変わる。世代番号(`solver_gen`)を上げると
  `should_stop` が True になり、`solver_bridge` が子プロセスを kill する。
- 経過秒を1秒ごとに出す(止まっていないと分かるように)。
- 残り搾取率が 2% を超えていたら「まだ収束しきっていない」と併記する。

### 分かっている制限

1. **ポストフロップ専用。** プリフロップはチャートのまま。
2. **2人用。** 相手が2人以上の局面は解けない。
3. **レンジがポジション任せ。** `strategy.solver_ranges()` が
   オープンレンジ相当を自動で作っているだけ。
   相手の癖に合わせるには169マスのレンジ入力UIが要る。**次はここ。**
4. フロップの82秒は実戦中に待てる時間ではない。
   ターン・リバーは実用的なので、フロップは検討用と割り切る。

---

## 追記2: ポーカーチェイス(トーナメント)対応(2026-09-09)

「今のままでポーカーチェイスの何ランクまで行けるか」という話から、
**そもそも前提が違う**ことが分かって作り替えた。

- ポーカーチェイスのランク戦は **6人テーブルのターボトーナメント**。
  ブラインドが約3分ごとに上がり、アンティあり、**順位でポイントが増減する**。
- こちらはキャッシュゲーム前提で、**チップの期待値**しか見ていなかった。
  終盤で「チップEVはプラスだが順位EV的には降りるべき」場面を全部押してしまう。
  ランク戦で一番ポイントを失う負け方をする作りになっていた。

### 順位ポイント(実測値)

STAGE V: `1位 +35 / 2位 +21 / 3位 +7 / 4位 -6 / 5位 -19 / 6位 -28`
別の資料では `+34 / +17 / +3 / -3 / -17 / -34`。両方プリセットに入れてある。
レート差による補正(自分と場の平均の差40につき±1程度)は入れていない。
判断の向きが変わる大きさではないため。

### icm.py

Malmuth-Harville。「チップ量に比例した確率で1位が決まり、その人を除いて繰り返す」。

実装は**部分集合のDP**にした。最初は順列(6人で720通り)を回す実装を書いたが、
自分の順位が決まった時点で打ち切るための係数計算がややこしく、間違えやすい。
`taken[S] = 集合Sが上位|S|位を占める確率` を 64通りぶん先に作る方が短くて確実。

検証は `test_icm.py`。勝率と違って公表された正解値が無いので、
**満たすべき性質**を見ている。

- 全員同じスタック → 各順位が均等
- 確率の合計が1(ランダム200通り、ずれ 2e-16)
- 1位になる確率 = 自分のチップ / 全員のチップ
- **勝って増える価値より、負けて失う価値の方が大きい**(19.43 > 10.24)
  ← ICM の本質。ここが逆なら実装が間違っている
- ICM込みの必要勝率 > チップ基準の必要勝率(43.8% > 40.0% など)

### チップの流れ(confrontation_stacks)

決着後のチップ量を4通り(勝ち/負け/相手が降りる/自分が降りる)返す。
**どの結末でもチップの総量が変わらない**ことをテストで確かめている。
ここがずれると ICM が静かに嘘をつくので、保存則のテストは外さないこと。

### pushfold.py

ターボは浅くなるとオールインするか降りるかの二択になるので、専用に計算する。

**ハマったところ**: 最初、後ろに残っている全員に同じコールレンジを当てていたら、
BTN の10BB で KTo も QJs も「降りる」になった。実戦の感覚と違いすぎる。
原因は、**ブラインド以外の人まで広くコールする**扱いにしていたこと。
ブラインドは既にチップを置いているぶん広く受けるが、それ以外は降りる。
`NON_BLIND_RATIO = 0.45` を掛けて分けたら全員降りる確率が妥当になった。

なお KTo/QJs/87s は **チップ基準ならオールイン、順位ポイント基準なら降り** で、
これは修正後も変わらない。これは間違いではなく **ICM が効いている証拠**。
チップEVで押しすぎるのを止めるのがこの改修の目的そのもの。

| ハンド(10BB BTN) | チップ基準 | 順位ポイント基準 |
| --- | --- | --- |
| KTo | +0.63BB 押し | -0.59pt 降り |
| QJs | +0.95BB 押し | -0.19pt 降り |
| 87s | +0.58BB 押し | -0.66pt 降り |
| A5s | +1.24BB 押し | +0.17pt 押し(僅差) |

### 分かっている制限

1. **ICM は実力差・ポジション・ブラインドの上がり方を見ていない。**
   チップ量の比だけのモデル。ターボはブラインドの上がりが速いので、
   本当は「次の周でどれだけ削られるか」も効くはずだが入っていない。
2. **勝負相手は1人固定。** マルチウェイのポットは順位EVに反映されない。
3. **オールイン判断はコールが1人までの近似。** 囲まれる分だけ評価が甘い。
4. コールレンジは固定表。相手の癖に合わせられない。
5. **プリフロップのレンジ表はキャッシュ用のまま。** ターボのトーナメント用
   (もっと浅い前提)には差し替えていない。25BB以下なら pushfold.py の方を見る。

---

## 追記3: ブラインドコストとレンジ表の生成(2026-09-09)

「強くするには何が必要か」の答えとして、効きの大きい順に

1. プリフロップのレンジ表がキャッシュ用のまま(特に25〜40BB帯が空白)
2. ブラインドの上がりを見ていない(ICMに時間の概念が無い)

の2つを片付けた。どちらも「浅いスタックのプリフロップ」の話なのでまとめて。

### ブラインドコスト(icm.after_blinds)

ICM は静的なモデルで、**降りて待っていればチップが減らない**ことになっている。
ターボでは3分ごとにブラインドが上がるので、これは実態とかけ離れている。

`Situation.orbit_cost()` = `1.5 + アンティ x 人数` を、
**すべての結末**(勝ち/負け/相手が降りる/自分が降りる)に適用する。
自分が払う分だけを引いて他の生き残りに均等に配る(他家同士は相殺とみなす)。

全部の結末に同じ額を掛けるので一見打ち消し合いそうだが、
**ICM は非線形なので打ち消さない**。短いスタックほど 2BB の重みが大きいため、
「今動く」ことの価値が正しく上がる。

効果は大きい。10BB・BTN、アンティ 0.1BB(1周 2.1BB)で:

| ハンド | ブラインドコスト無し | 有り |
| --- | --- | --- |
| KTo | -0.38pt 降り | **+0.42pt 押し** |
| QJs | +0.10pt 押し(僅差) | **+0.91pt 押し** |
| 87s | -0.44pt 降り | **+0.37pt 押し** |

追記2の時点では「ICMのせいで一般的なチャートより辛い」状態だったが、
**辛かったのはICMではなくブラインドコストの欠落だった**。
入れたら一般的なプッシュ/フォールド表の広さに近づいた。

### ranges_gen.py — レンジ表を計算で作る

表を探してくるのではなく、`icm.py` と `pushfold.py` の上で計算する。
169ハンドそれぞれについて「降りる / 2.2BBにレイズ / オールイン」を
順位EVで比べ、一番良いものを採用する。

**ハマったところ(2回)**

1. **コールされた後を『そのままショーダウン』にしたら、169ハンド全部がレイズになった。**
   2.2BB 出して 2.1BB のポットを 58% の確率で取れる計算になるうえ、
   コールされても損失がレイズ額で止まる形だったため、72o まで +EV になった。
   → 「コール後もポットの半分くらいは追加で入る」「勝率は 82% しか実現しない」
   「7% は3ベットで降ろされる」を入れて修正。
2. **試行回数4000だと表にムラが出た**(中間の手が降りで、それより弱い手がレイズ)。
   順位EVが0付近のハンドはMCの誤差で答えが裏返る。
   → 2段構えにした。まず全部を4000回で粗く見て、
   **順位EVの絶対値が 0.15pt 未満のハンドだけ 24000回で引き直す**。
   全部を高精度でやると数分かかるが、割れるのは境目だけなのでそこだけで済む。
   これで 15秒 → 71秒。ムラは消えた。

**BTN・30BB の結果**: 参加 102表記(キャッシュ表は95)。
キャッシュ表になくて生成側にあるのは K3o〜K7o、Q6o、Q8o、J8o、T8o など
**オフスーツのブロードウェイ寄り**。逆に生成側で落ちるのは 43s、53s、63s、
74s、84s、87o といった**スーテッドの繋がり**。

これは理屈と合っている。浅いとポストフロップで伸ばす価値(インプライドオッズ)が
無くなるので、スーテッドコネクターの価値が落ち、
そのままショーダウンで強いカードの価値が上がる。

### GUI

トーナメント欄に「このスタックのレンジ表を作る」ボタン。別スレッドで走り、
進捗%が出て、途中で「中止」できる。できると13x13の表が別ウィンドウで開き、
**自分の今のハンドが枠で強調される**。キャッシュ用チャートとのズレも下に出る。

### まだ残っていること

- **マルチウェイの順位EV**(勝負相手は今も1人固定)
- レンジ表の生成は 1〜2分かかるので、対局中には作れない。事前に作る想定
- ポストフロップの近似(実現率82%・追加投資ポットの半分)は当てずっぽうの値。
  ソルバーと突き合わせて較正できるはずだが、まだやっていない

---

## 追記4: マルチウェイと相手レンジの絞り込み(2026-09-09)

残っていた弱点のうち、確実に効く2つを潰した。

### ソルバーでの較正はできなかった

最初にやろうとしたのは「ポストフロップ近似の値(実現率82%など)を
ソルバーで較正する」だったが、**TexasSolver の dump_result は
戦略の頻度しか出力しない**(ノードのキーは `actions` と `strategy` のみで EV が無い)。
EV が読めないので、実現率を実測できない。

代わりに手はある。木の全ノードの戦略は入っているので、
**戦略に従って手を最後まで打ち回すシミュレーションを書けば実測できる**。
ただし全ストリートをダンプする必要があり(`set_dump_rounds` を増やす)、
実レンジだとJSONが数百MBになる恐れがあるので今回は見送った。
**実現率82%・追加投資はポットの半分・3ベット7% は、いまも当てずっぽうのまま。**
`ranges_gen.py` が一番よく使われる部分なので、ここは残った最大の未検証点。

### マルチウェイの順位EV(icm.multi_outcomes)

`confrontation_stacks()` の一般形。相手が複数いるポットで、
勝ったとき/それぞれの相手が勝ったときのチップの動きを全部出す。
自分が負けたとき誰が取るかは分からないので、呼び出し側で全員ぶんを平均する。

テストで押さえた性質:

- 複数人でもチップの総量が変わらない
- 相手が多いほど、勝ったときの期待ポイントも高い(8.11 > 4.75)
- **相手1人なら2人用の計算と完全に一致**(既存の挙動を壊していない)

セットで3人相手にオールインする局面で、順位EVが +10.30pt → +18.65pt に変わった。
1人固定のときは他の2人から取るチップを数え落としていた。

### 相手のレンジをストリートで絞る

`Situation.villain_action` に「なし / コールしてきた / 打ってきた」を持たせ、
プリフロップのレンジをそこから狭める。

**ハマったところ**: 最初はどちらも「強い方から上位X%」で絞っていた。
コールならそれで正しいが、**ベットは強い手とブラフの二極**なので、
上から40%を取ると相手が全部本物になってしまう。
AK で Q72 のフロップ、CO のベットに対して勝率が **19.3%** という
明らかにおかしい数字が出た。

`_narrow()` を二極対応にした(強い方から28% + 弱い方から14%)。

| 相手の行動 | 自分(AK)の勝率 |
| --- | --- |
| なし | 50.6% |
| コールしてきた | 31.1% |
| 打ってきた | **36.2%** (修正前 19.3%) |

「打ってきた」方が「コールしてきた」より勝率が高いのは正しい。
ベットにはブラフが混ざるが、コールは弱い手が落ちて手が締まるため。

この絞り込みは効果が非常に大きい(勝率が20ポイント動く)。
逆に言うと、**入れる前は情報を1つも使っていなかった**ということでもある。

---

## 追記5: ソルバー結果のキャッシュ(2026-09-09)

「通信を許せば速くなるのか」という質問から。答えは **通信は速さの理由ではない**。
クラウドのGTOサービスが即答なのは、あらかじめ解いた解答を大量に貯めてあるから
(GTO Wizard は ICMファイナルテーブルだけで5万3千局面)。
**同じ手はローカルでもできる**ので、まずキャッシュを入れた。

### 効く理由

**ソルバーの解は自分の手札に依存しない。**
決まるのは (ボード・両者のレンジ・ポット・スタック・ベットサイズ・精度) だけで、
出力の木には全ハンドぶんの戦略が入っている。
つまり **手札を変えただけなら解き直す必要がまったく無い**。

実測:

| | 時間 |
| --- | --- |
| 1回目(ターン) | 22.5秒 |
| 2回目・同じ設定 | **0.03秒** |
| **手札だけ変更** | **0.01秒** |

キー はソルバーに渡すコマンド文字列の SHA-1。`solver_engine/cache/` に貯める
(.gitignore 済み)。壊れたファイルは読めなかった時点で捨てて解き直す。
GUI に件数とサイズを出し、消すボタンも付けた。

### engine の載せ替えについて(調査中)

TexasSolver を選んだ理由は「オフライン + Windows のビルド済みバイナリ + 無料」で、
**オフラインは制約になっていなかった**。より速い無料の選択肢もローカルにある。

- [postflop-solver](https://github.com/b-inary/postflop-solver) (Rust):
  作者の比較では PioSOLVER や GTO+ を上回り、
  TexasSolver は「実行が遅くメモリ効率が悪い」と名指しされている。
  **ただし2023年10月で開発停止、かつライブラリなのでCLIが無い**。
  使うには Rust で小さな実行ファイルを書く必要がある。
- [opensolver](https://github.com/JoakimMich/opensolver) (Rust):
  UPI(Universal Poker Interface)対応でCLIから叩ける。
  ただしアイソモーフィズム未実装で商用比2倍遅い。

このPCに Rust は入っていないので、akochan と同じく Docker でビルドする方針。

### precompute.py — 寝ている間に解かせる

キャッシュが効くのは「同じ設定をもう一度解くとき」なので、
**先に解いておけば初回から即答になる**。そのための道具。

フロップは 22100 通りあり全部は解けない。次に同じボードが出る確率もほぼ無い。
GTO の勉強で昔から使われている手に倣って、
**テクスチャごとの代表フロップだけを解く**(ハイカード/ミドル/ロー・ペア・
モノトーン・繋がりの有無)。`REPRESENTATIVE_FLOPS` を書き換えれば
自分がよく負けるボードに差し替えられる。

途中で止めても解けた分は残る。もう一度走らせるとキャッシュにある分は
「済み(再利用)」と出して飛ばすので、そのまま続きから進む。

---

## 追記6: 実現率の較正を試して、失敗した(2026-09-09)

`ranges_gen.py` の実現率 0.82 が当てずっぽうのままだったので、
ソルバーから実測しようとした。**測定自体は成功したが、この用途には使えなかった。**
残しておかないと同じことを繰り返すので、経緯を全部書く。

### 測定はできた(calibrate.py)

ソルバーは EV を出力しない(ノードのキーは actions と strategy だけ)。
そこで **戦略に従って手を最後まで打ち回すシミュレーション** を書いた。
木には全ノード・全ハンドの行動頻度が入っているので、それに従って
サイコロを振れば実際に何チップ取れるかが測れる。

ダンプのサイズを先に測ってから設計した:

| 局面 | ダンプ範囲 | サイズ | 時間 |
| --- | --- | --- | --- |
| ターン | 全ストリート | 29MB | 28秒 |
| フロップ | フロップ+ターン | 31MB | 81秒 |
| フロップ | 全ストリート | **3356MB** | 604秒 |

フロップからの全ストリートは無理。ターンからなら全部入る。

実現率の定義は `取り分 / (素の勝率 x ポット)`。
チェックダウンするとちょうど 1.0 になる定義なので、基準として素直。
同じシミュレーションの中で同じリバーカードを使って
「チェックダウンしていたら勝っていたか」も数えるので、分子と分母が
同じ標本から出る(乱数のブレが打ち消し合う)。

結果:

| | 実現率 |
| --- | --- |
| ターン以降(リバーまで全部) | IP **1.088** |
| フロップ以降(リバーはチェックダウン) | IP **1.26** |
| 同上・OOP(チップ保存から逆算) | **0.79** |

**当てずっぽうの 0.82 は符号から間違っていた。** ポジションがある側は
素の勝率**以上**に取る。検算も合う(IP 3.30 + OOP 2.70 = ポット 6.00)。

### それでも使えなかった理由

測定値をそのまま入れたら **169ハンド全部がレイズ** になった。
2段階で原因を潰したが、最後の壁は越えられなかった。

1. **二重計上**。実現率はベットで降ろして取る分を既に含んでいるのに、
   モデル側でも「勝てばポット全部」と数えていた。
   → 「取り分の期待値が実測と一致するように勝率を逆算する」形に直した。
2. **1つの定数にしたこと**。実現率は手の強さで大きく変わる。
   強さ別に測り直したら、比ではなく取り分そのものを見る必要があると分かった
   (弱い手は分母の勝率が小さく、比が跳ね上がって使い物にならない)。
   取り分で見ると U 字型になる。**弱い手は降りれば損失0なので取り分は正、
   中間の手が一番実現が悪く、強い手が大きく取る**。実戦感覚と一致する。
3. **越えられなかった壁: 循環している。**
   測れるのは「**解かれたレンジの中での**その手の取り分」であって、
   「**そのレンジにその手を足したとき**の取り分」ではない。
   弱い手が正の取り分を持つのは、同じレンジに強い手が入っていて
   ブラフが通るからで、その値を根拠にレンジを広げるのは筋が通らない。
   広げた瞬間にレンジ全体の性質が変わり、前提が崩れる。

### 結論

- `REALIZATION = 0.82` は**実測値ではなく当てはめの定数**として残す。
  生成されるレンジの広さが一般的なチャートに収まるように選んだもの
  (BTN 30BB で 54%)。**ここは今も最大の未検証点**。コメントにそう書いた。
- `calibrate.py` は残す。測定そのものは正しく、
  「IPは1.26、OOPは0.79」という別の知見が得られた。ポストフロップの
  判断を評価するときには使える。
- 正しく較正するには、**レンジ自体をソルバーに解かせる**しかない
  (プリフロップソルバー)。TexasSolver はポストフロップ専用なのでできない。
